// JavaScript

$(function () {
  $(".bxslider").bxSlider({
    // 'horizontal', 'vertical', 'fade'
    mode: "fade",
    auto: true,
    // play/pause - 실행/일시정지
    // autoControls: true
    stopAutoOnClick: true,
    // pagenation(indicator) 
    // -> 하단 중앙의 동그라미 버튼
    // pager: false,
    slideWidth: 1903,
  });
});
